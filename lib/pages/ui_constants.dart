import 'package:flutter/cupertino.dart';

const EdgeInsets pagePadding = EdgeInsets.symmetric(
  horizontal: 24,
  vertical: 28,
);

const EdgeInsets fieldPadding = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 18,
);

const EdgeInsets listTilePadding = EdgeInsets.symmetric(
  horizontal: 20,
  vertical: 15,
);

const double listIconSize = 34;
